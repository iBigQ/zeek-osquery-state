[package]
description = State reconstruction of SQL tables for osquery hosts.
tags = osquery, state
version = 0.3.5
depends =
  zeek-osquery-framework ~0.3.3
  zeek-osquery-queries ~0.3.4
script_dir = scripts
