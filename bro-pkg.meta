[package]
description = State reconstruction of SQL tables for osquery hosts.
tags = osquery, state
version = 0.3.2
depends =
  zeek-osquery-framework ~0.3.2
  zeek-osquery-queries ~0.3.2
script_dir = scripts
