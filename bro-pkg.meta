[package]
description = State reconstruction of SQL tables for osquery hosts.
tags = osquery, state
version = 0.3.6
depends =
  zeek-osquery-framework ~0.3.4
  zeek-osquery-queries ~0.3.5
script_dir = scripts
